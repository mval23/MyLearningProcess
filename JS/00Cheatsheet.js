1. Variables: 

- Declare a variable: `var variableName;`
- Assign a value to a variable: `variableName = value;` or `var variableName = value;`
- Variable types: `number`, `string`, `boolean`, `null`, `undefined`, `object`, `function`, `array`

2. Data types:

- Numbers: `var x = 5;`
- Strings: `var x = "Hello";`
- Booleans: `var x = true;`
- Arrays: `var x = ["apple", "banana", "orange"];`
- Objects: `var person = {name:"John", age:30, city:"New York"};`
- Null: `var x = null;`
- Undefined: `var x;`

3. Operators:

- Arithmetic: `+`, `-`, `*`, `/`, `%`
- Comparison: `==`, `===`, `!=`, `!==`, `>`, `<`, `>=`, `<=`
- Logical: `&&`, `||`, `!`
- Assignment: `=`, `+=`, `-=`, `*=`, `/=`, `%=`
- Conditional: `? :`

4. Conditional statements:

- If statement: 

```
if (condition) {
  // Code to be executed if condition is true
} else {
  // Code to be executed if condition is false
}
```

- Switch statement:

```
switch(expression) {
  case x:
    // Code to be executed if expression matches x
    break;
  case y:
    // Code to be executed if expression matches y
    break;
  default:
    // Code to be executed if expression doesn't match any case
}
```

5. Loops:

- For loop: 

```
for (initialization; condition; increment) {
  // Code to be executed
}
```

- While loop: 

```
while (condition) {
  // Code to be executed
}
```

- Do-while loop: 

```
do {
  // Code to be executed
} while (condition);
```

6. Functions:

- Function declaration: 

```
function functionName(parameters) {
  // Code to be executed
}
```

- Function expression: 

```
var functionName = function(parameters) {
  // Code to be executed
}
```

7. Arrays:

- Accessing elements: `arrayName[index]`
- Changing elements: `arrayName[index] = newValue`
- Length of an array: `arrayName.length`
- Adding elements: `arrayName.push(value)`
- Removing elements: `arrayName.pop()`

8. Objects:

- Accessing properties: `objectName.propertyName` or `objectName["propertyName"]`
- Adding properties: `objectName.newProperty = value`
- Removing properties: `delete objectName.propertyName`

9. Miscellaneous:

- Commenting: `// Single-line comment` or `/* Multi-line comment */`
- Printing to console: `console.log(value)`
- Prompts: `var x = prompt("Enter a value:");`
- Alerts: `alert("Message to be displayed");`

This is just a brief overview of JavaScript. For more detailed information, please refer to official JavaScript documentation or online tutorials.